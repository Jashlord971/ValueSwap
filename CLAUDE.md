# Working in this repo

## No comments in code

Do not add comments to any code in this repo — Rust, JS, CSS, HTML, TOML,
anything. This applies to every edit and every new file, not just when
explicitly asked. If code needs explanation, make the explanation part of
your chat response instead of writing it into the file. Existing comments
that get touched incidentally should be removed rather than preserved.
